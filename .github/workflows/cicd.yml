name: cicd

on:
  workflow_dispatch:

jobs:
  code:
    runs-on: ubuntu-latest
    steps:
      - name: clone the code
        run: echo "colne the code"

  build:
    runs-on: ubuntu-latest
    steps:
      - name: build using docker
        run: "build uding docker"

  test:
    runs-on: ubuntu-latest
    steps:
      - name: test the code
        run: echo "test the code"
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: deploy the code
        run: echo "deploy code"
      
