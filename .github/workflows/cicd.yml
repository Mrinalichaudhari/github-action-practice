name: cicd

on:
  workflow_dispatch:

jobs:
  code:
    runs-on: ubuntu-latest
    steps:
      - name: clone the code
        run: echo "colne the code"

  build:
    needs: [code]
    runs-on: ubuntu-latest
    steps:
      - name: build using docker
        run: echo "build uding docker"

  test:
    needs: [build]
    runs-on: ubuntu-latest
    steps:
      - name: test the code
        run: echo "test the code"
  deploy:
    needs: [code,build]
    runs-on: ubuntu-latest
    steps:
      - name: deploy the code
        run: echo "deploy code"
      
